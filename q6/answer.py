
#class node:
#    def __init__(self, parent, left=None, right=None):
#        self.parent = parent
#        self.childern= filter(lambda child: child is not None, [left, right])
#    def __eq__(self, other_node):
#
#
#def createTree(breadth_first_string):
#    all_numbers = breadth_first_string.split(",")
#    childern = []
#    if len(all_numbers) > 1:
#        childern = all_numbers[1:]
    #return node(all_numbers[0], *childern)

#    nodes = {all_numbers[0]: None}
    #return nodes
    ##numbers_in_level = 2

    #for number in numbers:
    #    this_level = all_numbers[0:numbers_in_level]
    #    all_numbers = all_numbers[numbers_in_level:]
    #    nodes


    #    numbers_in_level *= 2


